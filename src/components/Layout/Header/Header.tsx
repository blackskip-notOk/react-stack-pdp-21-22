import { FC } from "react";
import { Layout } from "antd";

export const Header: FC = () => {
    const { Header } = Layout;

    return (
        <Header>
            Header
        </Header>
    );
}